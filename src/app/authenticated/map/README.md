# Map (shared)

Radius and boundary map search used by **Farming** and **Statistics** sidebars.

- Page: [`pages/map-search/`](pages/map-search/) (`MapSearchComponent`)
- Routes: `/farming/radius-search`, `/farming/boundary-search`, `/statistics/radius-search`, `/statistics/boundary-search`
- Services: [`services/`](services/) (`OlMapService`, map scripts loader, table sync)

When working on farming or statistics map search, start in `map-search.component.ts` here.
